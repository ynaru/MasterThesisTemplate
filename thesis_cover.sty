\makeatletter

\def\StudentIdNumber#1{\def\@StudentIdNumber{#1}} % 学籍番号
\def\seireki#1{\def\@seireki{#1}} % 西暦での年度
\def\seifuku#1{\def\@seifuku{#1}} % 正本か副本か
\def\supervisor#1{\def\@supervisor{#1}} % 指導教員
\def\cosupervisor#1{\def\@cosupervisor{#1}} % 副指導教員
\def\labname#1{\def\@labname{#1}} % 研究室名

\renewcommand{\maketitle}{%
  \begin{titlepage}%
    \let\footnotesize\small
    \let\footnoterule\relax
    \let\footnote\thanks
    \null\vfil
    \vskip 30\p@
    \begin{center}
      {\huge 修士学位論文 }
      {\Large \@seifuku \par}%
      \vskip 4em%
      % {\LARGE \@title \par}%
      % {\huge 高輝度LHC-ATLAS実験に向けた\\ミューオントリガー論理回路の実装及び性能評価\par}
      {\fontsize{23pt}{10pt}\selectfont 高輝度LHC-ATLAS実験に向けた\par}
      \vskip 0.8em%
      {\fontsize{23pt}{20pt}\selectfont ミューオントリガー論理回路の実装及び性能評価 \par}
      \vskip 1.3em%
      {\fontsize{21pt}{20pt}\selectfont --- トリガー系の統合と試験システムの構築 --- \par}
      \vskip 2.3em

      \[
        \left(
        \begin{tabular}{c}
          \fontsize{16pt}{10pt}\selectfont Implementation and performance evaluation of the muon trigger logic\\[6pt]
          \fontsize{16pt}{10pt}\selectfont for the High-Luminosity LHC-ATLAS experiment\\[10pt]
          \fontsize{14pt}{10pt}\selectfont --- Integration of the trigger logic and development of its validation framework ---\\
        \end{tabular}
        \right)
      \]      

      % {\fontsize{16pt}{10pt}\selectfont Implementation and performance evaluation of the muon trigger logic\par}
      % \vskip 0.5em
      % {\fontsize{16pt}{10pt}\selectfont for the High-Luminosity LHC-ATLAS experiment \par}
      % \vskip 1.0em
      % {\fontsize{14pt}{10pt}\selectfont - Integration of the trigger
      % logic and development of its validation framework -}%
      \vskip 10em%
      {\large
        \begin{tabular}[t]{c}%
          東京大学大学院\\
          理学系研究科 物理学専攻\\
          奥村研究室
        \end{tabular}\par}%
      \vskip 1em%
      {\large
        \begin{tabular}[t]{c}%
          % 博士課程（前期課程）2年\\
          % 学籍番号 \@StudentIdNumber \\
          \\
            {\Large \@author}
        \end{tabular}\par}%
      \vskip 1.5em
      {\large \@date \par}%
    \end{center}%
    \par
    \@thanks\vfil\null
  \end{titlepage}%
  \setcounter{footnote}{0}%
  \global\let\thanks\relax
  \global\let\maketitle\relax
  \global\let\@thanks\@empty
  \global\let\@author\@empty
  \global\let\@date\@empty
  \global\let\@title\@empty
  \global\let\title\relax
  \global\let\author\relax
  \global\let\date\relax
  \global\let\and\relax
}%

\makeatother
